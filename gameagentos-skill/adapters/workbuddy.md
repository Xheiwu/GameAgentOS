# WorkBuddy Adapter

## Purpose

This adapter describes how GameAgentOS is exposed as a WorkBuddy Skill.

The runtime entry file is SKILL.md.
It requires YAML frontmatter with:

- name
- description
- version

The core GameAgentOS content remains platform independent.

Runtime adapters only provide loading compatibility.
